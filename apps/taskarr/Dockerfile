# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build taskarr`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t taskarr`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system taskarr && \
          adduser --system -G taskarr taskarr

COPY dist/apps/taskarr taskarr/
RUN chown -R taskarr:taskarr .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix taskarr --omit=dev -f install

CMD [ "node", "taskarr" ]
